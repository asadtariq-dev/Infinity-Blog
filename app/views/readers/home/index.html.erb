<%= render 'shared/navbar', locals: {home: true} %>
<% if @posts.any? %>
  <%= render 'shared/blogs', posts: @posts %>
<%else%>
  <%= render 'shared/empty' %>
<% end %>
<% if @comments.any? %>
  <h4>Recent Comments</h4>
  <%= render 'shared/comments_list', comments: @comments%>
<% end %>
