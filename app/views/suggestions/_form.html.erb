
<form action="<%= post_suggestions_path(post)%>" method="post">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <% if !parent.nil? %>
    <input type="hidden" name="suggestion[parent_id]" value="<%= parent.id %>">
  <% end %>
  <textarea class="form-control mb-2 bg-transparent" name="suggestion[content]" placeholder="Type here..."></textarea>
  <input type="submit" name="Submit" class='btn btn-sm btn-primary'>
</form>
